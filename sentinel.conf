# specify path to chaincoin.conf or leave blank
# default is the same as ChaincoinCore
#chaincoin_conf=/home/evan82/.chaincoincore/chaincoin.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
